<%= form_for @author do |f| %>
    <%= f.label :name %><%= f.text_field :name %><br />
    <%= f.fields_for :books, @author.books.build do |b| %>
        　<%= b.label :title %><%= b.text_field :title %>
    <% end -%>
    <br />
    <%= f.fields_for :books, @author.books.build do |b| %>
        　<%= b.label :title %><%= b.text_field :title %>
    <% end -%>
    <%= f.submit %>
    <br />
<% end %>
